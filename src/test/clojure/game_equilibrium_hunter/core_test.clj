(ns game-equilibrium-hunter.core-test
  (:require [clojure.test :refer :all]
            [game-equilibrium-hunter.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (not (= 0 1)))))
